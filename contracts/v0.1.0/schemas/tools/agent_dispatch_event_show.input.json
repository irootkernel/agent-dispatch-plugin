{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:agent-dispatch-plugin:contracts:v0.1.0:input:agent_dispatch_event_show",
  "title": "agent_dispatch_event_show input",
  "description": "aggregate_id is required and preserved literally after validation.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "aggregate_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:@-]{0,127}$",
      "maxLength": 128
    }
  },
  "required": ["aggregate_id"]
}
