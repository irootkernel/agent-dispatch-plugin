{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:agent-dispatch-plugin:contracts:v0.1.0:input:agent_dispatch_schedule_inspect",
  "title": "agent_dispatch_schedule_inspect input",
  "description": "route_id is required and preserved literally after validation. The platform is fixed to launchd by the command mapping.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "route_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:@-]{0,127}$",
      "maxLength": 128
    }
  },
  "required": ["route_id"]
}
